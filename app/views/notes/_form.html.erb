<%= form_with(model: [task, note], url: task_notes_path(task)) do |form| %>
  <div class="field">
    <%= form.label :body, 'Text' %>
    <%= form.text_area :body, rows: 1 %>
  </div>

  <div class="actions">
    <%= form.submit 'Add Note' %>
  </div>
<% end %>
